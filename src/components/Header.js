import React, { Component } from "react";

class Header extends Component {
    render(){
        return (
            <header className="uk-padding uk-background-muted">
                Conteudo do header
            </header>
        )
    }
}

export default Header;